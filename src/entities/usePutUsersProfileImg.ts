export const usePutUsersProfileImg = () => {

   const putUsersProfileImg = ( profileImg ) => {
      if (!profileImg) return;
      // api요청이후 상태코드를 반환하여 재요청하지 않고 변경된 이미지를 보여주는걸로 처리예정
   };
   return { putUsersProfileImg };
};
